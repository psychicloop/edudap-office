
# EduDAP Office (Flask) – Final

- Employees see **only their own data**.
- Leave calendar with **drag-to-select** creation.
- Quotations library: **PDF/Excel** parsing + **FTS keyword search** + **Bulk Delete** + **Bulk Replace Image**.
- Admin panel to manage everything.

## Run
```bash
pip install -r requirements.txt
cp .env.example .env
python app.py
```
Open `/login` in your browser.
